  GNU nano 7.2                                           ProAct6.cpp
#include <iostream>
using namespace std;

int main() {
    int N, suma = 0;

    // Solicitar al usuario el valor de N
    cout << "Ingrese un número natural N: ";
    cin >> N;

    // Validar que el número ingresado sea positivo
    if (N > 0) {
        // Calcular la suma de los primeros N números naturales
        for (int i = 1; i <= N; i++) {
            suma += i;
        }

        // Mostrar el resultado
        cout << "La suma de los primeros " << N << " números naturales es: " << suma << endl;
    } else {
        cout << "Error: Ingrese un número natural positivo." << endl;
    }

    return 0;
}
